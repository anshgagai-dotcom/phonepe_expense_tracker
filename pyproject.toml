[project]
name = "phonepe-expense-tracker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Ansh Gandhare", email = "anshgag.ai@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "pywebview>=6.2.1",
]

[project.scripts]
phonepe-expense-tracker = "main:main"

[tool.uv]
package = false

[dependency-groups]
dev = [
    "pillow>=12.3.0",
    "pyinstaller>=6.22.2",
]
